packages:
  - packages/*
  - apps/*
overrides:
  esbuild: ^0.28.1
allowBuilds:
  # Office→PDF auth uses only firebase/app + firebase/auth (precompiled). These
  # build scripts aren't needed (protobufjs is firestore-only), so skip them.
  '@firebase/util': false
  esbuild: true
  protobufjs: false
  sharp: false
  workerd: true
