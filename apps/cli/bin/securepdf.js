#!/usr/bin/env node
// Thin launcher. The real entry lives in src/index.ts and is wired up in
// Milestone 3 (built to ./dist before publish).
console.error('securepdf CLI is not implemented yet (Milestone 3).')
process.exit(1)
